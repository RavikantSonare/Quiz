<Window x:Class="ExamSimulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExamSimulator" Loaded="Page_Loaded"
        mc:Ignorable="d" MouseDown="Windows_MouseDown"
        Title="MainWindow" Height="455" Width="725" 
        WindowStyle="None" ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen"  
        AllowsTransparency="True" BorderBrush="#A5BBC8" BorderThickness="6"
        Icon="Resources/q.ico">
    <Window.Resources>
        <Style x:Key="WindowIconStyle" TargetType="{x:Type Button}">
            <Setter Property="FontFamily" Value="Webdings" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="25"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="TextBlock.TextDecorations" Value="{x:Null}" />
            <Setter Property="Cursor" Value="{x:Null}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Gray" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="btnEnable" TargetType="{x:Type Button}">
            <Style.Triggers>
                <DataTrigger Binding ="{Binding ElementName=listFile, Path=SelectedIndex}" Value="-1">
                    <Setter Property="Button.IsEnabled" Value="false"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <Border Height="70" Background="#3B6688" DockPanel.Dock="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*">
                    </ColumnDefinition>
                    <ColumnDefinition Width="6*"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label x:Name="label" Grid.Row="0" Grid.Column="0" Content="Exam Simulator" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="30"/>
                <Label Margin="10,40,10,0" Grid.Row="0" Grid.Column="0" Content="Demo-Only" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="15"/>
                <Menu Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" FontSize="18" Background="#3B6688" Foreground="White">
                    <MenuItem Header="File">
                        <MenuItem Header="Add"/>
                        <MenuItem Header="Remove" />
                        <MenuItem Header="Exit" />
                    </MenuItem>
                    <MenuItem Header="Exam">
                        <MenuItem Header="Start"  />
                        <MenuItem Header="Select All"  />
                    </MenuItem>
                </Menu>
                <Rectangle  HorizontalAlignment="Stretch" Height="35" VerticalAlignment="Top" Grid.Column="2"/>
                <Button Style="{StaticResource WindowIconStyle}" x:Name="Btnclose" Content="r" Grid.Row="0" Grid.Column="2" Click="TriggerClose"/>
                <Button Style="{StaticResource WindowIconStyle}" x:Name="Btnmin" Content="0" Margin="0,0,25,0"  Grid.Row="0" Grid.Column="2" Click="TriggerMinimize"/>
            </Grid>
        </Border>
        <Border Height="50" Background="#0C4068" DockPanel.Dock="Bottom">

        </Border>
        <Border Height="50" DockPanel.Dock="Bottom">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Row="0" Grid.Column="1">
                    <Button x:Name="btnStudyMode" Height="40" Margin="10,7,10,0" Background="Orange"  Foreground="White" FontSize="20" Click="btnStart_Click" CommandParameter="{Binding ElementName=listFile,Path=SelectedItem}" Style="{StaticResource btnEnable}" ToolTip="SM">Study Mode</Button>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="2">
                    <Button x:Name="btnTestMode" Height="40" Margin="10,7,10,0" Background="Orange"  Foreground="White" FontSize="20" Click="btnStart_Click" CommandParameter="{Binding ElementName=listFile,Path=SelectedItem}" Style="{StaticResource btnEnable}" ToolTip="TM">Test Mode</Button>
                </StackPanel>

            </Grid>
        </Border>
        <Border Width="200" Background="White" DockPanel.Dock="Right" BorderThickness="0,0,0,1" BorderBrush="Gray">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <StackPanel Grid.Column="0">
                    <Button Height="40" Margin="10,7,10,0" Background="Orange" Foreground="White" FontSize="20" Name="btnDownload" Click="btnDownload_Click">Download</Button>
                    <Button Height="40" Margin="10,7,10,0" Background="#29577B" Foreground="White" FontSize="20" Name="btnAdd" Click="btnAdd_Click">Add</Button>
                    <Button Height="40" Margin="10,7,10,0" Background="#29577B" Foreground="White" FontSize="20" Name="btnRemove" Click="btnRemove_Click" CommandParameter="{Binding ElementName=listFile,Path=SelectedItem}" Style="{StaticResource btnEnable}">Remove</Button>
                </StackPanel>
            </Grid>
        </Border>
        <Border Background="White">
            <ListBox x:Name="listFile" Width="auto" ScrollViewer.VerticalScrollBarVisibility="Visible"  >
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="#FFFACD"/>
                        <Setter Property="Foreground" Value="Orange"/>
                        <Setter Property="FontFamily" Value="Georgia"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" >
                            <Image Source="Images/docs-icon.png" />
                            <Label x:Name="lblExam1"  Content="{Binding Title}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </DockPanel>

</Window>
